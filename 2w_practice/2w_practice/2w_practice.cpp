#include <iostream>
#include <iomanip>

using namespace std;

int main()
{
	//quiz 3

	//int a;
	//int b;

	//cin >> a >> b;
	//cout.setf(ios::left);
	//cout << setw(20) << "a/b" << " = " << a / b << endl;
	//cout << setw(20) << "a%b" << " = " << a % b << endl;
	//cout << setw(20) << "a/(float)b" << " = " << a / (float)b << endl; //강제 형변환. 값 둘중 하나라도 int가 아니면 계산값도 int가 아니게 된다.
	//cout << setw(20) << "a/(double)b" << " = " << a / (double)b << endl;
	//cout << a << "와 " << b << " 중 큰 값" << " = ";
	//if (a > b) {
	//	cout << a;
	//}
	//else {
	//	cout << b;
	//}

	// include <iomanip> 와  setf, setw의 사용

	
	//quiz 4

	/*int n = 0;
	int int_max = 0;
	while (n >= 0) {
		n = n + 1;
		if (n > 0) int_max = n;
	}
	cout << "int overflow\n";
	cout << "maximum of int: " << int_max << "\n";*/


	//quiz 5

	/*int a;
	cout << sizeof(a) << endl;*/

	// int, char, double, float, short 타입 변수의 사이즈 출력

}

